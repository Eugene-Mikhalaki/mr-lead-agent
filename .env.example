# GitLab
GITLAB_BASE_URL=https://gitlab.example.com
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Repository (set once, then only --mr-iid needed per run)
REPO_URL=https://gitlab.example.com/group/repo.git
# MR_IID=42  # can also be set here, but changes every run

# Context Retrieval (How many code snippets to include in prompt)
# 12 is optimal. Max 100. Set to 0 to disable context retrieval (diff only).
# MAX_CONTEXT_FRAGMENTS=12

# LLM Provider: 'gemini' | 'deepseek' | 'openrouter' | 'groq'
LLM_PROVIDER=groq

# Google Gemini (used if LLM_PROVIDER=gemini)
GEMINI_API_KEY=AIzaSy-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# DeepSeek (used if LLM_PROVIDER=deepseek)
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# DEEPSEEK_MODEL=deepseek-chat  # default: deepseek-chat (V3), or deepseek-reasoner (R1)

# OpenRouter (used if LLM_PROVIDER=openrouter) — free tier available!
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENROUTER_MODEL=qwen/qwen3-coder:free  # default — специализирован для кода
# Other free models (актуальные на фев 2026):
#   meta-llama/llama-3.3-70b-instruct:free
#   openai/gpt-oss-120b:free
#   nousresearch/hermes-3-llama-3.1-405b:free
#   mistralai/mistral-small-3.1-24b-instruct:free
#   google/gemma-3-27b-it:free

# Groq (used if LLM_PROVIDER=groq) — бесплатный, очень быстрый!
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GROQ_MODEL=llama-3.3-70b-versatile  # default
# Other Groq models:
#   llama-3.1-70b-versatile
#   deepseek-r1-distill-llama-70b

# Review Settings
# REVIEWER_USERNAME=your.gitlab.username  # Your GitLab username (to exclude your own MR comments from prompt)
# REVIEW_LANGUAGE=ru                      # 'ru' for Russian, 'en' for English (default: en)
# REVIEW_RULES_FILE=./review_rules.md     # Path to coding rules file (default: ./review_rules.md)
